# 智财代账工作台（ZhiCai Workbench）项目书

> **版本**：v1.0 ｜ **日期**：2026-09-02 ｜ **文档性质**：开发蓝图（供 Codex 作为唯一需求输入进行代码开发与项目实现）
> **一句话定位**：借鉴 Odoo 开源 ERP 的架构思想与数据模型设计，为"一人代理记账公司管理 100 家客户账务"打造一套部署在本机、零外部服务依赖、主打自动记账与云端备份的 WebUI 工作台。

---

## 0. 文档使用说明（Codex 必读）

1. 本文档是**唯一需求来源**（Single Source of Truth）。实现时若发现本文档内部矛盾，按以下优先级裁决：**第 6 章数据模型 > 第 7 章功能规格 > 第 8 章 UI 规范 > 其他章节**。
2. 开发节奏：**严格按第 10 章里程碑顺序实施，一次只做一个里程碑（M1 → M7），每个里程碑完成并通过验收标准（DoD）后才进入下一个**。禁止跳步、禁止提前实现后续里程碑的功能。
3. 所有代码、注释、提交信息、界面文案：**代码与标识符用英文，界面文案与注释用简体中文**。
4. 技术栈与目录结构在第 4、5 章已定死，**不得自行更换框架或重构目录结构**。
5. 每个里程碑交付时必须包含：可运行代码 + 自动化测试（pytest）+ README 更新（运行方式与变更说明）。
6. 遇到本文档未覆盖的业务细节，按"中国小企业会计准则 + 税务常识"做合理默认实现，并在 README 的"假设与决策记录"小节中登记，**不要停下来提问**。

---

## 1. 项目概述

### 1.1 背景与痛点

使用者是一家一人经营的代理记账公司（老板 = 唯一操作者 = 会计 = 管理员），同时为约 100 家中小微企业提供代理记账与报税服务。当前工作方式的痛点：

| # | 痛点 | 具体表现 |
|---|------|---------|
| P1 | 客户与期限管理散乱 | 100 家客户的资料、合同到期日、各税种申报期限分散在 Excel、微信、脑子里，容易漏报、漏收 |
| P2 | 凭证手工录入效率低 | 每月数千张发票、银行流水靠手工录凭证，重复劳动占比极高 |
| P3 | 多客户切换繁琐 | 每换一家客户就要切换一套账，缺少统一总览与批量处理视角 |
| P4 | 外贸客户流程特殊 | 需对接单一窗口、电子口岸、外汇等平台，报关/收汇/退税资料容易断档 |
| P5 | 数据安全无保障 | 客户账务数据在单机 Excel/旧软件中，无自动备份，硬盘故障即灾难 |
| P6 | 专业软件重且贵 | 主流代账软件按客户数收费、年费高，且很多功能为多人团队设计，单人用不动 |

### 1.2 项目目标

- **G1 统一工作台**：一个浏览器界面管完 100 家客户的档案、合同、记账、报税、收费、文档。
- **G2 自动记账**：发票、银行流水按规则自动生成凭证（借鉴 Odoo 的 account.reconcile 自动对账模型思想），把手工录入量降到 20% 以下。
- **G3 不漏不迟**：申报日历 + 任务看板，每客户每税种每属期的申报状态一目了然。
- **G4 外贸专区**：报关单、出口发票、收汇结汇、出口退税全流程台账。
- **G5 本机部署 + 云备份**：系统跑在自己电脑上（无外网依赖、数据不出本机），SQLite 单文件数据库每日自动备份到 OneDrive 同步目录。
- **G6 Odoo 级的架构品质**：模块化、模型驱动 UI（Metadata-driven UI）、多账套隔离、操作审计——把 Odoo 二十年沉淀的 ERP 架构精华移植到轻量技术栈上。

### 1.3 范围界定（Scope）

**做（In Scope）**：
- 单机 Web 应用（本机浏览器访问 localhost），单用户为主、预留多角色。
- 100 家客户账套的数据隔离与全局总览。
- 借鉴 Odoo 架构的自研系统（详见第 3 章"借鉴策略"）。

**不做（Out of Scope，第一版明确排除）**：
- 不直接对接税局/海关的 API 自动申报（政策与安全原因，系统只做**计算底稿 + 状态台账 + 官网快捷入口**，实际申报由用户在电子税务局/单一窗口人工完成，完成后回系统登记状态）。
- 不做多用户实时协同、不做移动 App（Web 响应式布局可被手机浏览器访问即可）。
- 不做进销存、生产制造、电商等 Odoo 其他领域模块。

---

## 2. 业务全景分析

### 2.1 角色画像

| 角色 | 说明 | 权限 |
|------|------|------|
| admin（老板本人） | 唯一日常使用者，全权 | 所有模块读写 + 系统设置 |
| accountant（预留） | 未来可能雇的助理会计 | 记账/发票/申报读写，无系统设置与删除权限 |
| viewer（预留） | 只读（如给客户演示） | 只读 |

> 当前实际只有 admin 一个账号。权限模型必须按上述三角色建好（借鉴 Odoo 的 groups + record rules 思想），但 UI 上不复杂化。

### 2.2 核心业务流程

**流程 A：月度做账流（每月每客户，占工作量 70%）**

```
收集资料（发票/银行回单/工资表）
  → 登记/导入进项发票、销项发票
  → 导入银行流水
  → 自动记账引擎批量生成凭证 → 人工补录调整凭证
  → 凭证审核过账（posted）
  → 期末结转损益（一键）
  → 出报表：资产负债表 / 利润表 / 科目余额表 / 总账 / 明细账
```

**流程 B：申报流（每月/每季）**

```
申报日历提醒（某客户某税种某属期 → 未申报）
  → 系统生成申报底稿（自动计算应纳税额，含计算过程）
  → 用户登录电子税务局人工申报（系统提供直达链接）
  → 回系统标记"已申报"，登记缴款金额与日期
  → 台账状态流转：未开始 → 待申报 → 已申报未缴款 → 已完成
```

**流程 C：外贸客户流（部分客户）**

```
出口业务发生 → 录入报关单（或从单一窗口导出数据后导入）
  → 开出口发票 → 收汇登记（币种/金额/汇率）
  → 结汇/待核查 → 出口退税资料归档 → 免抵退税台账
```

**流程 D：收费流（代账公司自身收入）**

```
客户合同（代账费标准/周期）→ 到期提醒 → 开票/收款登记 → 应收统计
```

### 2.3 数据量估算（性能设计的依据）

| 数据对象 | 单客户/月 | 100 客户/月 | 10 年累计 |
|----------|----------|------------|----------|
| 凭证（move） | ~40 张 | ~4,000 张 | ~48 万 |
| 凭证行（move_line） | ~160 行 | ~16,000 行 | ~192 万 |
| 发票（进+销） | ~50 张 | ~5,000 张 | ~60 万 |
| 银行流水 | ~60 条 | ~6,000 条 | ~72 万 |
| 附件（图片/PDF） | ~100 个 | ~10,000 个 | 文件系统存储，仅元数据入库 |

**结论**：总行量在百万级，SQLite（WAL 模式）绰绰有余；关键查询路径（book_id + date、account_id）必须建索引。单表无分表必要。

---

## 3. Odoo 借鉴策略（本项目最重要的架构决策）

### 3.1 为什么不直接部署 Odoo 本体，而是"架构师从、实现自建"

对"以 Odoo 为基座"的两种路线做了评估：

| 维度 | 路线 A：直接用 Odoo 社区版二次开发 | 路线 B（本项目采用）：借鉴 Odoo 架构，轻量栈自建 |
|------|----------------------------------|----------------------------------------------|
| 运行环境 | 必须 PostgreSQL + Python 3.11+ + 长驻多进程服务，本机还跑着 Ollama 大模型，内存吃紧 | Python + SQLite，单进程，常驻内存 < 200MB |
| 部署/运维 | 升级模块、数据库迁移、插件兼容问题，一人无 IT 运维扛不住 | 双击 start.bat 即用，出问题删库重跑 |
| 中国本地化 | Odoo 社区版中国会计本地化（l10n_cn）能力薄弱，中国式凭证/申报台账仍要大量定制 | 从第一天就按中国代账业务建模 |
| 许可证 | Odoo 社区版 LGPL-3.0：直接复制/修改其源码文件，修改部分须按 LGPL 开源（内部使用虽无碍，但定制模块发布受限） | 只借鉴其**架构思想与数据模型设计**（不受版权保护的思想层面），代码 100% 自研重写，不复制任何 Odoo 源码文件，无许可证包袱 |
| 功能匹配度 | Odoo 强项在电商/制造/库存/协同，对"一人管 100 家客户账"是 90% 冗余 | 100% 按代账业务裁剪 |

**决策：采用路线 B。** "以 Odoo 为基座"的含义 = 把 Odoo 最有价值的**六大架构资产**作为设计蓝本（见 3.2），而不是把它的代码跑起来。

### 3.2 从 Odoo 借鉴的六大架构资产

**资产 1：模块化架构（Odoo addons 机制）**

Odoo 中"一切皆模块"：每个模块是一个目录，含 `models/`（业务对象）、`views/`（界面定义）、`security/`（权限）、`data/`（初始化数据），由 `__manifest__.py` 声明依赖。模块可插拔、可叠加扩展。

→ 本项目实现：`app/modules/<module_name>/` 目录约定，模块注册表机制（详见 5.3），每个模块自带 models、API 路由、视图配置（JSON）、种子数据，按依赖顺序自动加载。

**资产 2：数据模型设计（Odoo account / contacts 模块的模型体系）**

Odoo 的会计模型是业界教科书级的复式记账实现：

- `res.partner`：统一的客户/供应商联系人模型（`is_company`、`vat` 税号等字段）
- `account.account`：科目表（按公司隔离）
- `account.journal`：账簿/日记账（sale/purchase/bank/cash/misc 五类）
- `account.move`：**凭证与发票统一模型**（自 Odoo 13 起，发票、账单、手工凭证都是 account.move 的不同 `move_type`），状态机 draft → posted → cancel
- `account.move.line`：凭证行（`debit`/`credit`/`account_id`/`partner_id`），**借贷合计必须平衡（sum = 0）才能过账**，且"已过账凭证不可修改"（只能红冲/冲销）
- `account.tax`：税种定义（税率、税基类型）
- `res.company` + `company_id` 字段：**多公司机制**——同一套表、每行数据带公司 ID、查询自动过滤

→ 本项目照搬这套模型设计并做中国化改造（凭证字、摘要、辅助核算、借贷方向红字冲销等），模型命名保持与 Odoo 一致的可对应关系（见第 6 章映射表）。

**资产 3：模型驱动 UI（Odoo 视图 = XML 配置而非手写页面）**

Odoo 的列表/表单/看板/日历/透视表视图是**数据（XML）**而非代码：开发者声明模型字段与视图配置，前端框架统一渲染。这使新增一个业务模块几乎不用写页面代码。

→ 本项目实现：视图配置 JSON（每模型声明 list/form 视图的字段、布局、筛选器、动作按钮），前端实现通用 `ListView` / `FormView` / `KanbanView` / `CalendarView` 渲染器消费这些配置。**这是本项目的核心工程杠杆：通用视图组件写一次，所有模块复用。**

**资产 4：ORM 使用范式（Odoo models.Model）**

Odoo ORM 的 recordset 风格：`search(domain)` 域过滤语法、`create/write/unlink`、计算字段（`@api.depends`）、约束校验（`@api.constrains`）、`_order` 默认排序、`_rec_name` 记录显示名。

→ 本项目实现：SQLAlchemy 2.0 declarative + 自研轻量基类 `BaseModel`（见 5.3.2），提供 domain 过滤解析、`rec_name`、审计字段（create_uid/create_date/write_uid/write_date，Odoo 同名字段）等约定。**不重造完整 ORM，只吸收其约定。**

**资产 5：Chatter 与审计（Odoo mail.thread mixin）**

Odoo 每条业务记录底部有"沟通区"（chatter）：显示操作日志（谁何时改了什么字段）与内部留言。

→ 本项目实现：通用 `audit_log` 表（model + record_id + 变更 JSON）+ 通用 `note` 表，FormView 底部统一渲染"活动日志"面板。

**资产 6：自动化规则（Odoo automated actions / reconcile models）**

Odoo 支持声明式自动动作（条件触发服务端动作）和银行对账规则模型（`account.reconcile.model`：按流水描述匹配 → 自动生成凭证）。

→ 本项目实现：自动记账规则表（触发条件 + 凭证模板，见 7.7），以及通用"计划任务"框架（备份、逾期提醒，见 7.13）。

### 3.3 Odoo 源码参考索引（给 Codex 的阅读地图）

> 仅作为设计参考阅读，**禁止复制代码**。若需理解某行为的正确做法，优先读这些文件：

| 借鉴点 | Odoo 源文件（github.com/odoo/odoo，18.0 分支） |
|--------|---------------------------------------------|
| 凭证状态机与过账/红冲 | `addons/account/models/account_move.py` |
| 借贷平衡校验、行级约束 | `addons/account/models/account_move_line.py` |
| 科目表与科目类型 | `addons/account/models/account_account.py` |
| 账簿（journal）设计 | `addons/account/models/account_journal.py` |
| 银行对账规则（自动凭证） | `addons/account/models/account_reconcile_model.py` |
| 科目表模板/初始化 | `addons/account/models/chart_template.py` |
| 模块清单结构 | 任意模块的 `__manifest__.py` |
| 视图 XML 语法 | `addons/account/views/account_move_views.xml` |
| 多公司字段约定 | 任意模型中的 `company_id = fields.Many2one('res.company', ...)` |

---

## 4. 技术选型

### 4.1 技术栈总览（定稿，不可更改）

| 层 | 选型 | 版本 | 说明 |
|----|------|------|------|
| 语言 | Python | 3.12+ | 用户本机已有 Python 3.13 |
| Web 框架 | FastAPI | 最新稳定版 | 自动 OpenAPI 文档、Pydantic 校验、Codex 熟练度最高 |
| ORM | SQLAlchemy | 2.0（declarative） | 成熟稳定，配 Alembic 做迁移 |
| 数据库 | SQLite | 3.x，**开启 WAL 模式** | 单文件、零运维、可被 OneDrive 整体备份 |
| 数据校验 | Pydantic v2 | — | API 层 DTO |
| 前端框架 | Vue 3 | 3.4+，**CDN 全局构建（vue.global.prod.js），不用 Node 构建** | 理念与 Odoo OWL 同源（组件化/响应式/虚拟DOM），但生态与 Codex 熟练度远胜 OWL |
| UI 组件库 | Element Plus | 最新，CDN 引入 | 表格/表单/弹窗/日期选择器开箱即用 |
| 图表 | ECharts 5 | CDN 引入 | 仪表盘 |
| 认证 | 自研 session（itsdangerous 签名 cookie） | — | 单机应用不需要 OAuth/JWT 复杂度 |
| 测试 | pytest + httpx（AsyncClient） | — | 每模块单测 + API 集成测试 |
| 迁移 | Alembic | — | 表结构演进 |

**CDN 依赖说明**：前端第三方库走 CDN；同时在 `static/vendor/` 放一份本地副本 + 页面做"CDN 失败回退本地副本"，保证断网可用（`<script>` 依次尝试）。所有前端资源不允许 Node/npm/webpack/构建步骤。

### 4.2 选型理由（为什么不是别的）

- **为什么不用 Odoo OWL 前端**：OWL 是 Odoo 专用框架，社区小、文档少、Codex 训练数据覆盖差，产出质量风险高。Vue 3 与 OWL 设计理念一致（组件 + 响应式 + 虚拟 DOM），但生态成熟。
- **为什么 SQLite 不是 PostgreSQL**：数据量百万级（见 2.3）、单用户写入、无并发热点——SQLite WAL 完全胜任；换来的是"备份 = 复制一个文件"的极简运维，与 G5（云备份）直接对齐。
- **为什么 FastAPI 不是 Flask/Django**：Django 太重（admin/ORM/模板全套绑定）；Flask 能做但要手工拼的东西多。FastAPI + Pydantic + 自动文档，Codex 产出质量最稳。
- **为什么不用服务端模板渲染（Jinja2 全栈）**：本系统的交互复杂度（可编辑表格、拖拽看板、联动筛选）适合 SPA 模式；且"模型驱动 UI"需要前端持有视图配置动态渲染。

### 4.3 运行环境与部署

- 目标机器：用户 Windows 本机（同时运行 Ollama 等），内存预算 < 300MB。
- 启动方式：项目根目录 `start.bat` —— 检查 venv → 缺依赖则安装 → 启动 uvicorn（127.0.0.1:8000）→ 自动打开默认浏览器。
- 配置文件：`config.yaml`（数据库路径、备份目录、端口、会话密钥），首次启动自动生成默认值并引导初始化管理员账号。
- 数据目录：`data/zhicai.db`（SQLite 主库）+ `data/attachments/`（附件文件按 `YYYY/MM/<uuid>.<ext>` 组织，DB 只存元数据——与 Odoo 的 ir.attachment 文件存储策略一致）。

---

## 5. 系统架构设计

### 5.1 总体分层架构

```
┌─────────────────────────────────────────────────────────────┐
│  浏览器（Vue 3 + Element Plus，CDN 引入，无构建）              │
│  ├─ AppShell（左侧导航/顶栏/账套切换器/全局搜索）               │
│  ├─ 通用视图渲染器：ListView / FormView / KanbanView /        │
│  │   CalendarView / DashboardView（模型驱动，读视图配置JSON）   │
│  └─ 各模块少量定制页面（凭证录入、对账工作台、报表等）           │
├─────────────────────────────────────────────────────────────┤
│  FastAPI 应用层                                               │
│  ├─ /api/v1/meta/*   元数据 API（模型字段、视图配置、菜单）     │
│  ├─ /api/v1/data/*   通用 CRUD API（ORM 模型驱动的动态路由）    │
│  ├─ /api/v1/biz/*    业务动作 API（过账、结转、对账、算税…）    │
│  └─ 中间件：会话认证 / 当前账套注入 / 审计日志                  │
├─────────────────────────────────────────────────────────────┤
│  业务逻辑层（app/modules/<module>/service.py）                 │
│  ├─ 记账引擎（凭证生成/平衡校验/过账/红冲/结转）                │
│  ├─ 自动记账规则引擎                                           │
│  ├─ 申报计算引擎（增值税/所得税/印花税/附加…）                  │
│  └─ 备份/计划任务                                              │
├─────────────────────────────────────────────────────────────┤
│  数据层：SQLAlchemy 2.0 ORM + BaseModel 约定                   │
│  （模块注册表、domain 过滤解析、审计字段、book_id 隔离）         │
├─────────────────────────────────────────────────────────────┤
│  SQLite（WAL） + 附件文件系统 + OneDrive 备份目录               │
└─────────────────────────────────────────────────────────────┘
```

**关键设计原则：**

1. **通用 API + 模型注册表（Odoo ir.model 思想）**：每个 SQLAlchemy 模型在启动时注册到 `ModelRegistry`（表名、显示名、字段元数据、权限、默认视图配置）。FastAPI 动态生成 `/api/v1/data/<model>` 的 CRUD 路由；前端 ListView/FormView 通过 `/api/v1/meta/<model>` 拿到字段与视图配置后渲染。**新增一个业务实体的开发成本 ≈ 定义一个 SQLAlchemy 模型 + 一份视图配置 JSON。**
2. **账套隔离中间件（Odoo 多公司 record rules 思想）**：所有带 `book_id` 的模型，查询自动注入 `book_id == 当前账套` 过滤；显式声明"跨账套查询"才能绕过（仅仪表盘总览、客户列表等白名单场景）。
3. **业务动作与 CRUD 分离**：状态流转（过账、审核、对账、算税）一律走 `/api/v1/biz/*` 的显式动作端点，禁止前端直接改状态字段。

### 5.2 项目目录结构（定稿）

```
zhicai/                          # 项目根目录
├── start.bat                    # Windows 一键启动
├── requirements.txt             # fastapi uvicorn sqlalchemy alembic pydantic itsdangerous pyyaml httpx pytest
├── config.yaml                  # 运行配置（首次自动生成）
├── README.md                    # 含"假设与决策记录"小节
├── alembic/                     # 数据库迁移
├── app/
│   ├── main.py                  # FastAPI 入口：装配中间件/路由/启动钩子
│   ├── core/                    # 框架层（对标 Odoo odoo/ 核心包）
│   │   ├── registry.py          # 模型注册表 + 视图配置加载
│   │   ├── base_model.py        # SQLAlchemy BaseModel（审计字段/book_id 约定/rec_name）
│   │   ├── domain.py            # Odoo 风格 domain 过滤语法解析 → SQLAlchemy 条件
│   │   ├── security.py          # 会话认证 + 角色权限检查
│   │   ├── book_context.py      # 当前账套上下文（中间件注入）
│   │   ├── audit.py             # 通用审计日志（对标 Odoo mail.thread）
│   │   ├── sequence.py          # 自动编号（对标 Odoo ir.sequence）
│   │   └── scheduler.py         # 计划任务（备份/提醒，对标 Odoo ir.cron）
│   ├── api/
│   │   ├── meta_api.py          # /api/v1/meta/*
│   │   ├── crud_api.py          # /api/v1/data/<model> 动态 CRUD
│   │   └── deps.py              # 依赖注入（db session / 当前用户 / 当前账套）
│   └── modules/                 # 业务模块（对标 Odoo addons/）
│       ├── base/                # 用户/角色/设置/审计日志查看
│       ├── res_partner/         # 客户档案（对标 Odoo contacts）
│       ├── contract/            # 代账合同与收费
│       ├── account/             # 记账核心（对标 Odoo account）
│       ├── invoice/             # 进销项发票
│       ├── bank/                # 银行流水与对账
│       ├── auto_entry/          # 自动记账规则引擎
│       ├── tax_decl/            # 申报台账与申报日历
│       ├── foreign_trade/       # 外贸专区
│       ├── payroll/             # 工资社保
│       ├── documents/           # 文档中心
│       ├── tasks/               # 任务与提醒
│       └── board/               # 首页仪表盘
│           每个模块目录约定：
│           ├── __init__.py      # register() 返回模块描述符
│           ├── models.py        # SQLAlchemy 模型
│           ├── views.py         # 视图配置（Python dict，运行时即 JSON）
│           ├── service.py       # 业务逻辑
│           ├── router.py        # 业务动作 API
│           ├── seed.py          # 种子数据（科目表模板/默认规则/征期表）
│           └── tests/           # pytest 测试
├── static/                      # 前端（无构建，直接静态服务）
│   ├── index.html
│   ├── vendor/                  # CDN 库的本地回退副本
│   ├── css/app.css
│   └── js/
│       ├── app.js               # Vue 应用入口 + 路由（hash 路由）
│       ├── api.js               # fetch 封装（错误统一处理）
│       ├── components/          # 通用视图渲染器
│       │   ├── ListView.js      # 列表视图（筛选/分组/排序/批量/分页）
│       │   ├── FormView.js      # 表单视图（标签页/子表/活动日志）
│       │   ├── KanbanView.js
│       │   ├── CalendarView.js
│       │   └── DashboardView.js
│       ├── modules/             # 各模块的定制页面（凭证录入/对账/报表…）
│       └── common/              # 工具函数
├── data/                        # 运行时数据（gitignore）
│   ├── zhicai.db
│   └── attachments/
└── docs/                        # 本项目书及后续文档
```

### 5.3 核心机制详细设计

#### 5.3.1 模块注册表（对标 Odoo addons 装载机制）

```python
# app/core/registry.py 关键接口（示意）
@dataclass
class ModuleDescriptor:
    name: str                    # 'account'
    label: str                   # '记账'
    depends: list[str]           # ['base', 'res_partner']
    menu: list[dict]             # 菜单项（含图标、路由、权限）
    models: dict[str, type]      # 表名 → SQLAlchemy 模型类

# 启动时：按依赖拓扑排序加载各模块的 register()，
# 建立表（alembic 管演进）、注册模型元数据、收集菜单与视图配置。
# 禁止循环依赖；依赖顺序：base → res_partner → account → 其他。
```

#### 5.3.2 BaseModel 约定（对标 Odoo models.Model 字段约定）

```python
# app/core/base_model.py 所有业务模型继承此类
class BaseModel:
    id: Mapped[int] = primary_key()
    create_uid / create_date / write_uid / write_date   # 审计四字段（Odoo 同名）
    active: bool = True                                  # 归档而非删除（Odoo 约定）
    # 声明 rec_name: str 类属性 —— 该字段值作为记录的显示名
    # 声明 book_scoped: bool = True 且含 book_id 列的模型自动账套隔离
```

**删除策略**：业务记录一律软删除（active=False 置归档），物理删除仅限测试环境。已过账（posted）凭证不可删除、不可修改（只能生成红冲凭证），此约束在 service 层强制。

#### 5.3.3 Domain 过滤语法（对标 Odoo search domain）

前端筛选器统一产出、后端统一解析的元组语法：`["&", ["state", "=", "posted"], ["date", ">=", "2026-01-01"], ["name", "ilike", "记-2026"]]`。支持的运算符：`=, !=, >, >=, <, <=, ilike, in, between`；逻辑连接：`&(and 默认), |(or), !(not)`。实现于 `core/domain.py`，必须带完整单测（这是所有列表查询的地基）。

#### 5.3.4 自动编号（对标 Odoo ir.sequence）

`ir_sequence` 表（code, prefix, padding, next_number, book_id）。凭证号规则：`{凭证字}-{YYYYMM}-{0001}`，按 账套+账簿+月份 重置计数；合同编号、任务编号同理。并发用 SQLite 单写者特性天然安全。

#### 5.3.5 当前账套上下文（对标 Odoo 多公司切换）

- 顶栏"账套切换器"：下拉列出全部启用客户（可搜索），选中写入会话 cookie。
- 中间件解析 cookie → `request.state.book_id`；CRUD API 自动注入过滤（5.1 原则 2）。
- "总览模式"（book_id=all）：仅 dashboard、res_partner 列表、tasks、tax_decl 日历等白名单模型可用，由模块在视图配置中显式声明 `allow_cross_book: true`。

---

## 6. 数据模型设计（字段级定义）

> 命名沿用 Odoo 习惯以便对照源码；类型标注用 SQLAlchemy 2.0 风格。所有表含 5.3.2 的审计字段与 active，下文不再重复列出。**金额一律用 Decimal（SQLite 存 TEXT，精度 2 位小数；汇率 6 位）——禁止 float。**

### 6.1 模型总览与 Odoo 映射表

| 本项目模型（表名） | 对标 Odoo 模型 | 模块 | 一句话说明 |
|-------------------|---------------|------|-----------|
| res_users | res.users | base | 登录用户 |
| res_role | res.groups | base | 角色（admin/accountant/viewer） |
| ir_config | ir.config_parameter | base | 键值配置 |
| ir_sequence | ir.sequence | base | 自动编号 |
| audit_log | mail.message(tracking) | base | 字段级变更日志 |
| note_message | mail.message(note) | base | 记录备注/留言 |
| res_book | res.company | res_partner | **客户账套**（一家客户公司=一个账套） |
| res_partner | res.partner | res_partner | 客户的往来单位（客户/供应商） |
| contract_agreement | —（Odoo subscription 借鉴） | contract | 代账服务合同 |
| contract_fee_item | — | contract | 收费计划/收款记录 |
| account_account | account.account | account | 会计科目 |
| account_journal | account.journal | account | 账簿（凭证字） |
| account_move | account.move | account | 记账凭证 |
| account_move_line | account.move.line | account | 凭证分录行 |
| account_tax | account.tax | account | 税率定义（进项/销项） |
| account_period_close | account.period.lock | account | 期末结账/锁账 |
| invoice_bill | account.move(in/out_invoice) | invoice | 进项/销项发票登记 |
| bank_statement | account.bank.statement | bank | 银行对账单（导入批次） |
| bank_statement_line | account.bank.statement.line | bank | 银行流水行 |
| auto_entry_rule | account.reconcile.model | auto_entry | 自动记账规则 |
| auto_entry_log | — | auto_entry | 规则执行日志 |
| tax_decl_item | — | tax_decl | 申报台账行（客户×税种×属期） |
| tax_calendar_rule | — | tax_decl | 征期规则（生成台账） |
| ft_customs_decl | — | foreign_trade | 报关单 |
| ft_customs_line | — | foreign_trade | 报关单商品行 |
| ft_fx_receipt | — | foreign_trade | 收汇/结汇记录 |
| ft_export_refund | — | foreign_trade | 出口退税台账 |
| payroll_batch | hr.payslip.run | payroll | 工资表批次 |
| payroll_line | hr.payslip | payroll | 员工月工资行 |
| doc_document | documents.document | documents | 文档登记与标签 |
| task_task | project.task | tasks | 任务/待办 |
| board_card | board.board | board | 仪表盘卡片配置 |

### 6.2 base 模块

**res_users**：id, login(唯一), name, password_hash(pbkdf2), role_id→res_role, active, last_login_at
**res_role**：id, code(admin/accountant/viewer), name, permissions(JSON：模型×读/写/删/动作 的矩阵)
**ir_config**：id, key(唯一), value(JSON)
**ir_sequence**：见 5.3.4
**audit_log**：id, model, record_id, user_id, action(create/write/post/…), changes(JSON 数组：`[{field, old, new}]`), created_at
**note_message**：id, model, record_id, user_id, body(Text), created_at

### 6.3 res_book —— 客户账套（本项目的心脏，对标 Odoo res.company）

一家代理的客户公司 = 一个账套。字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| code | str(20) 唯一 | 账套编号（如 C001） |
| name | str(200) | 客户公司全称 |
| short_name | str(50) | 简称（顶栏切换器显示） |
| credit_no | str(32) | 统一社会信用代码/税号 |
| taxpayer_type | enum | general(一般纳税人) / small(小规模纳税人) |
| is_foreign_trade | bool | 是否外贸客户（true 时解锁外贸专区菜单） |
| industry | str(50) | 行业 |
| legal_person / phone / address | str | 法人、电话、注册地址 |
| accounting_standard | enum | small(小企业会计准则) / enterprise(企业会计准则) |
| vat_period | enum | monthly / quarterly（增值税申报周期，按纳税人类型默认） |
| bookkeeping_start | date | 接账起始月份（期间控制） |
| bank_accounts | JSON | 开户行账户列表 `[{bank, account_no, alias}]` |
| charge_status | enum | normal / paused / terminated（服务状态） |
| remark | text | 备注 |
| rec_name | → short_name | |

> **约束**：新建账套时自动执行"科目表初始化"（从 seed 模板克隆整套科目，见 6.6）+ 生成默认账簿（4 个凭证字，见 6.7）+ 生成默认申报台账规则（见 7.9）。

### 6.4 res_partner —— 往来单位（对标 Odoo res.partner）

客户账套下的应收应付往来对象（该客户的客户和供应商）。字段：
name(必填), partner_type(enum: customer/supplier/both), tax_no, is_company(bool, 默认 true), phone, address, bank_account, remark, book_id。
列表默认按 name 排序；凭证录入时"辅助核算-往来单位"从此表联想选择。

### 6.5 account 模块 —— 记账核心（字段级定义）

**account_account（科目）**

| 字段 | 类型 | 说明 |
|------|------|------|
| code | str(20) | 科目编码（如 1001、220201），账套内唯一 |
| name | str(100) | 科目名称 |
| account_type | enum | asset_current/asset_fixed/asset_depreciation/liability_current/liability_long/equity/revenue/cost/expense/other |
| direction | enum | debit(借方) / credit(贷方) 余额方向 |
| is_leaf | bool | 是否末级（仅末级可记账） |
| parent_id | FK→account_account | 上级科目（构成科目树） |
| is_cash_flow | bool | 是否现金流量表科目 |
|辅助核算标志 | auxiliary_flags | JSON：`{"partner": true, "department": false, "project": false}` |
| book_id | FK | 科目表按账套隔离（对标 Odoo per-company CoA） |

**account_journal（账簿/凭证字）**

| 字段 | 类型 | 说明 |
|------|------|------|
| code | str(10) | 记/收/付/转 |
| name | str(50) | 记账凭证/收款凭证/付款凭证/转账凭证 |
| journal_type | enum | general/receipt/payment/transfer（对标 Odoo journal 类型思想） |
| book_id | FK | 每账套默认生成 4 个 |

**account_move（记账凭证）**

| 字段 | 类型 | 说明 |
|------|------|------|
| name | str(30) | 凭证号（sequence 生成：`记-202609-0001`） |
| journal_id | FK→account_journal | 账簿/凭证字 |
| move_date | date | 凭证日期 |
| period | str(7) | 所属期间 `YYYY-MM`（冗余存储，报表按此过滤） |
| state | enum | draft / posted / voided（红冲后状态） |
| source_type | enum | manual / auto_invoice / auto_bank / auto_payroll / closing / reversal（来源，自动记账可追溯） |
| attachment_count | int | 附件张数（会计凭证要素） |
| remark | text | 备查说明 |
| reversed_move_id | FK→account_move | 红冲目标凭证（红冲时生成新凭证反向分录并互链） |
| book_id | FK | — |

**account_move_line（凭证分录行）**

| 字段 | 类型 | 说明 |
|------|------|------|
| move_id | FK→account_move | 所属凭证（级联删除，仅 draft） |
| line_no | int | 行号 |
| summary | str(200) | **摘要（必填，中国凭证要素）** |
| account_id | FK→account_account | 科目（仅末级） |
| partner_id | FK→res_partner | 往来单位（科目开了 partner 辅助核算时必填） |
| department / project | str(50) | 其他辅助核算维度 |
| debit | Decimal(18,2) | 借方金额（≥0） |
| credit | Decimal(18,2) | 贷方金额（≥0） |

**service 层强制约束（对标 Odoo account.move 的 posting 约束）**：
1. 过账（post）前校验：`sum(debit) == sum(credit)` 且总额 > 0；行数 ≥ 2；每行 debit/credit 互斥（不能同时 >0）；摘要/科目必填；科目为末级；期间未锁（见 account_period_close）。
2. posted 后：整单只读，唯一出路是"红冲"（生成一张全反向凭证，两张凭证 state 标记互链）。
3. draft 可任意编辑/删除。

**account_period_close（期末结账）**：id, book_id, period, closed(bool), closed_at, note。已锁期间禁止新增/修改凭证（跨期补录需先解锁并留审计）。

**account_tax（税率定义）**：id, book_id, name(如"销项税 13%"), tax_kind(enum: vat_output/vat_input/surtax/stamp/…), rate(Decimal(6,4)), active。供发票模块和申报计算引用。

### 6.6 科目表种子模板（小企业会计准则核心科目）

内置 `account/seed_coa_small.py`（小企业会计准则）与 `seed_coa_enterprise.py`（企业会计准则），新建账套时按 accounting_standard 克隆。小企业准则核心一级科目（编码遵循现行准则）：

```
资产类：1001 库存现金 / 1002 银行存款 / 1012 其他货币资金 / 1101 短期投资 /
1121 应收票据 / 1122 应收账款 / 1123 预付账款 / 1131 应收股利 / 1132 应收利息 /
1221 其他应收款 / 1231 坏账准备 / 1401 材料采购 / 1403 原材料 / 1405 库存商品 /
1471 存货跌价准备 / 1501 长期债券投资 / 1601 固定资产 / 1602 累计折旧 /
1604 在建工程 / 1605 工程物资 / 1606 固定资产清理 / 1701 无形资产 / 1702 累计摊销 /
1801 长期待摊费用 / 1901 待处理财产损溢 / 1621 生产性生物资产（按需）
负债类：2001 短期借款 / 2201 应付票据 / 2202 应付账款 / 2203 预收账款 /
2211 应付职工薪酬 / 2221 应交税费 / 2231 应付利息 / 2232 应付利润 /
2241 其他应付款 / 2401 递延收益 / 2501 长期借款 / 2701 长期应付款
共同/权益类：3001 实收资本 / 3002 资本公积 / 3101 盈余公积 / 3103 本年利润 / 3104 利润分配
成本类：5001 生产成本 / 5101 制造费用 / 5201 劳务成本
损益类：6001 主营业务收入 / 6051 其他业务收入 / 6111 投资收益 / 6301 营业外收入 /
6401 主营业务成本 / 6402 其他业务成本 / 6403 税金及附加 / 6601 销售费用 /
6602 管理费用 / 6603 财务费用 / 6711 营业外支出 / 6801 所得税费用
```

关键二级科目随模板预置：2221.01 应交增值税(销项税额/进项税额/已交税金/转出未交增值税)、2221.02 未交增值税、2221.03 应交企业所得税、2221.04 应交个人所得税、2221.05 应交印花税、2221.06 应交城建税、2221.07 应交教育费附加、2221.08 应交地方教育附加、2221.10 应交出口退税；6601/6602/6603 下预置常用三级明细（办公费/差旅费/业务招待费/工资/社保/折旧/利息支出/手续费等）。**科目表可在账套内自由增改。**

### 6.7 invoice 模块 —— 发票（中国化设计，对标 Odoo account.move 的发票形态）

**invoice_bill（发票登记）**：一张表管进项+销项（对标 Odoo 用 move_type 区分发票形态的做法）。

| 字段 | 类型 | 说明 |
|------|------|------|
| direction | enum | input(进项) / output(销项) |
| invoice_type | enum | special(专票) / normal(普票) / electronic(数电票) / other |
| invoice_code / invoice_no | str | 发票代码 / 号码 |
| invoice_date | date | 开票日期 |
| period | str(7) | 所属期间 |
| partner_name / partner_tax_no | str | 开票方/受票方名称与税号（快照式存储） |
| partner_id | FK→res_partner | 可选关联往来单位 |
| goods_amount | Decimal | 不含税金额 |
| tax_amount | Decimal | 税额 |
| total_amount | Decimal | 价税合计（= goods + tax，自动计算） |
| tax_rate | Decimal(6,4) | 税率（13%/9%/6%/3%/5%/0%） |
| category | str(50) | 费用类别（办公/差旅/房租/材料…，用于自动记账科目映射） |
| state | enum | registered / entry_generated(已生成凭证) / voided(作废/红冲) |
| move_id | FK→account_move | 生成的凭证（追溯） |
| remark / attachment_id | — | 备注、影像附件 |
| book_id | FK | — |

支持：手工录入、Excel/CSV 批量导入（提供标准模板下载）、数电票 XML 批量导入（M1 只做手工+Excel）。**查重**：同 book 内 invoice_code+invoice_no+invoice_date 重复时警告。

### 6.8 bank 模块 —— 银行流水与对账

**bank_statement（对账单批次）**：id, book_id, bank_alias, account_no, period, imported_at, line_count, total_debit, total_credit。
**bank_statement_line（流水行）**：id, statement_id, trade_date, trade_no, counterpart_name, summary(交易摘要), debit(收入), credit(支出), balance(交易后余额), state(enum: unreconciled/reconciled), move_id(FK，生成的凭证)。

对账工作台（7.8）从流水行发起"生成凭证"。

### 6.9 auto_entry 模块 —— 自动记账规则（对标 Odoo account.reconcile.model）

**auto_entry_rule**：

| 字段 | 类型 | 说明 |
|------|------|------|
| name | str | 规则名，如"销项专票→确认收入" |
| trigger | enum | invoice_output / invoice_input / bank_line / payroll |
| match_condition | JSON | 过滤条件（对发票：`{"direction":"output","invoice_type":"special","tax_rate":"0.13"}`；对流水：`{"summary_ilike":"工资","debit_gt":0}` 等） |
| journal_code | str | 生成凭证的凭证字（默认"记"） |
| line_template | JSON | 分录模板（见下方） |
| priority | int | 多规则匹配时的优先级（小者优先） |
| enabled | bool | 启用开关 |

**line_template 结构**（支持变量替换）：
```json
[
  {"side": "debit",  "account": "1122 应收账款",      "summary": "应收 {partner_name}", "amount": "{total_amount}"},
  {"side": "debit",  "account": "2221.01.02 进项税额", "summary": "进项税 {tax_amount}", "amount": "{tax_amount}"},
  {"side": "credit", "account": "6001 主营业务收入",   "summary": "销售 {goods_amount}", "amount": "{goods_amount}"}
]
```
占位符来源：源单据字段值（发票/流水/工资行）；`{amount}` 支持表达式（如 `={goods_amount}*0.13`）。**引擎必须保证生成凭证借贷平衡，不平衡则整单拒绝并写日志。**

**auto_entry_log**：rule_id, source_model, source_id, result(ok/failed), move_id, message, created_at。

内置种子规则（可停用/修改）：
1. 销项发票（任意类型）→ 借 应收账款{价税合计} / 贷 主营业务收入{不含税} + 贷 销项税额{税额}
2. 进项发票按 category 映射 → 借 费用科目{不含税} + 借 进项税额{税额} / 贷 应付账款{价税合计}（category→科目映射表存 ir_config，可维护）
3. 银行流水"摘要含'工资'且贷方支出" → 借 应付职工薪酬 / 贷 银行存款
4. 银行流水"摘要含'税'且贷方支出" → 借 应交税费-未交增值税 / 贷 银行存款
5. 银行流水"借方收入" → 借 银行存款 / 贷 应收账款（配合核销）
6. 工资批次审核后 → 计提与发放两张凭证模板

### 6.10 tax_decl 模块 —— 申报台账

**tax_calendar_rule（征期规则）**：id, tax_kind(enum: vat/cit quarterly/cit annual/iit/surtax/stamp/…), period_type(enum: monthly/quarterly/yearly), deadline_rule(str, 如 "next_month_15" / "quarter_next_month_15" / "annual_0531"), applies_to(JSON 过滤：`{"taxpayer_type":"small"}`), enabled。

种子规则：
- vat：小规模→季度，次季首月 15 日截止；一般纳税人→月度，次月 15 日
- cit 季度预缴：次季首月 15 日；cit 年度汇算：次年 5 月 31 日
- iit（个税）：次月 15 日
- 印花税：按季（季首 15 日）——按用户实际业务，税目仅买卖合同 0.3‰
- 附加税（城建 7%/5%/1%、教育附加 3%、地方附加 2%）：随增值税

**tax_decl_item（申报台账行 —— 本模块核心实体）**：id, book_id, tax_kind, period, due_date, state(enum: pending/preparing/submitted/paid/done/exempt), computed_amount(Decimal, 系统计算应纳额), declared_amount(实缴额), paid_date, calc_snapshot(JSON：计算底稿——取数口径、公式、明细), remark, source("auto"由规则批量生成/"manual"手工补)。

**生成逻辑**：计划任务每月 1 日（及季度/年度节点）扫描全部启用账套 × 适用征期规则 → 批量生成当期 tax_decl_item；已存在的跳过（幂等）。

### 6.11 foreign_trade 模块

**ft_customs_decl（报关单）**：id, book_id, decl_no(报关单号), export_date, trade_mode(enum: FOB/CIF/CFR…), currency, fx_rate(Decimal(10,4)), usd_amount, cny_total(人民币总额), customer_abroad(境外客户), goods_count(件数), remark。
**ft_customs_line（商品行）**：id, decl_id, hs_code(海关编码), goods_name, qty, unit, unit_price, amount。
**ft_fx_receipt（收汇/结汇）**：id, book_id, decl_id(FK 可空), receipt_date, currency, amount, fx_rate, cny_amount, kind(enum: receipt/verification 待核查/settlement 结汇), bank_fee, remark。
**ft_export_refund（出口退税台账）**：id, book_id, decl_id, period, kind(enum: 免抵退/免退), status(enum: collecting资料收集中/submitted已申报/approved已审批/received已到账), refund_amount, received_date, remark。

### 6.12 payroll 模块

**payroll_batch（工资批次）**：id, book_id, period, state(draft/confirmed/paid), employee_count, total_gross, total_net, social_base(社保基数汇总), remark。
**payroll_line（员工行）**：id, batch_id, employee_name, id_card_tail(证件后四位), gross_salary, social_employee(个人社保), social_employer(单位社保), fund_employer(单位公积金), iit(个税), net_salary(实发, 自动计算), remark。

> 个税按月度累计预扣法简化计算（累计预扣预缴公式按 2026 现行个税法税率表实现，税率表存 ir_config 可更新），MVP 允许手工覆盖计算值。

### 6.13 contract / documents / tasks / board 模块

**contract_agreement（代账合同）**：id, book_id, contract_no, sign_date, start_date, end_date, service_scope(JSON 多选：记账/报税/工商年检/出口退税…), fee_type(enum: monthly/quarterly/annual), fee_amount, payer_name, state(enum: active/expired/terminated), remark。
**contract_fee_item（收费计划）**：id, agreement_id, period, amount, due_date, state(enum: unpaid/invoiced/paid/overdue), paid_date, paid_amount, invoice_no。
**doc_document（文档登记）**：id, book_id, name, doc_type(enum: 营业执照/合同/申报回执/银行回单/报关单/其他), tag_list(JSON), attachment_id, source_model/source_id(与业务单据关联), remark。
**task_task（任务）**：id, book_id(可空=全局任务), title, description, due_date, priority(enum: low/normal/high/urgent), state(enum: todo/doing/done/cancelled), assignee_id(预留), source_model/source_id, reminder_sent(bool)。
**board_card（仪表盘卡片配置）**：id, card_key(enum: 见 7.2), position(int), config(JSON), enabled。

### 6.14 ER 关系核心链（一图流）

```
res_book 1 ─── N account_account（科目表按账套隔离）
res_book 1 ─── N account_journal
res_book 1 ─── N account_move 1 ─── N account_move_line N ─── 1 account_account
                                    account_move_line N ─── 1 res_partner
invoice_bill N ─── 1 account_move（发票→生成的凭证）
bank_statement 1 ─── N bank_statement_line N ─── 1 account_move
tax_decl_item N ─── 1 res_book（台账按账套）
ft_customs_decl 1 ─── N ft_customs_line；ft_fx_receipt N ─── 1 ft_customs_decl
payroll_batch 1 ─── N payroll_line
contract_agreement 1 ─── N contract_fee_item；contract_agreement N ─── 1 res_book
所有业务表 N ─── 1 res_book（除全局表）
```

---

## 7. 功能模块规格（界面行为与验收口径）

> 本章按模块给出功能点、交互与**验收标准（DoD）**。通用规则：所有列表视图支持筛选/排序/分页/导出 CSV；所有表单有必填校验与脏数据离开提示；所有删除为归档（active=False）。

### 7.1 登录与全局框架（AppShell）

- 首次运行向导：创建 admin 账号 → 设置数据目录与 OneDrive 备份目录 → 完成进入。
- 登录页：账号 + 密码；登录后进入工作台。
- **左侧导航栏**（借鉴 Odoo 应用菜单，可折叠）：分组显示模块菜单（工作台 / 客户与合同 / 记账 / 发票与银行 / 税务申报 / 外贸专区 / 工资 / 任务与文档 / 系统）。菜单项由模块注册表驱动——装了什么模块就显示什么（Odoo 同款机制）。外贸专区仅当至少一个账套 `is_foreign_trade=true` 时显示。
- **顶栏**：左侧当前页面标题面包屑；中部全局搜索（输入客户简称/凭证号/发票号，返回分类结果跳转）；右侧**账套切换器**（下拉搜索 100 家客户）+ 用户菜单（修改密码/退出）。
- 账套切换后：左侧模块不变，主区域数据全部切换到该账套；总览模式下仪表盘等白名单页面跨账套聚合。
- **DoD**：登录/登出/会话过期拦截（跳登录页）；账套切换 1 秒内完成；全局搜索回车直达。

### 7.2 首页工作台（Dashboard，借鉴 Odoo board）

仪表盘卡片（可配置显示与顺序）：

| 卡片 | 内容 |
|------|------|
| 本月概览 | 服务客户数（active 账套数）、本月待过账凭证数、本月已过账凭证数、本月新发票数 |
| 申报日历红牌 | 未来 7 天到期且 state∈{pending,preparing} 的申报项列表（客户×税种×截止日），点击直达 |
| 逾期预警 | 已逾期未申报、逾期未收代账费（contract_fee_item overdue） |
| 待办任务 | state∈{todo,doing} 的任务前 10 条 |
| 收款月历 | 本月应收代账费合计/已收/未收 |
| 各客户进度 | 表格：客户 × 本月（收票/凭证/申报/收费）四状态灯 —— 一屏看清 100 家谁没做完 |

- **DoD**：仪表盘总览模式跨账套聚合正确；每张卡片可点击穿透到对应列表页。

### 7.3 客户管理（res_book + res_partner）

- 客户列表（总览模式）：编号/简称/税号/纳税人类型/服务状态/外贸标记/合同到期日；支持按纳税人类型、状态筛选；行点击进入该客户详情页（自动切换账套上下文）。
- 客户详情页（FormView 标签页）：基本信息 / 科目表 / 账簿 / 申报设置 / 合同收费 / 往来单位 / 文档 / 活动日志。
- 新建客户向导：填基本信息 → 选会计准则 → 一键初始化（克隆科目模板 + 建账簿 + 建征期规则 + 生成当年台账）。
- 停用客户：charge_status→terminated 后从切换器隐藏，数据保留。
- **DoD**：新建客户后科目表完整（小企业准则一级+预置二级科目齐全，科目树可展开）；重复税号拦截；100 家客户时列表加载 < 500ms。

### 7.4 合同与收费管理（contract）

- 合同列表+表单：基本要素、服务范围多选、收费周期与金额；到期前 30 天自动生成"续约提醒任务"。
- 收费计划：按合同周期自动生成各期 contract_fee_item（幂等，可手动补）；状态流转 unpaid→invoiced→paid；逾期自动标 overdue 并生成催收任务。
- 收款登记：批量勾选多客户多期一起标记收款（一键场景：月末统一收 60 家）。
- 报表：应收统计（按客户/按月份）；本年度已收合计。
- **DoD**：自动生成收费计划不重复不遗漏；逾期判定（today > due_date 且 unpaid）正确。

### 7.5 记账核心（account）—— 最重模块

**凭证列表**：默认按期间倒序；筛选（期间/凭证字/状态/来源/摘要关键词/科目）；批量操作（批量过账/批量打印）。行内快览（点开展开分录，不进详情页）。

**凭证录入页（定制页面，不用通用 FormView）**：
- 表头：凭证字（下拉）、日期、附件数；
- 分录表格（类 Excel 连续录入）：摘要（支持上一行摘要按 `//` 复制）、科目（编码/名称拼音联想，仅末级）、辅助核算（往来单位等按科目配置动态出现）、借方/贷方金额（输入一侧自动清另一侧）；回车自动加行；
- 底栏实时显示"借方合计 / 贷方合计 / 差额"，不平衡时【保存】可用但【保存并过账】禁用（红色差额提示）；
- 常用凭证模板：任意凭证可存为模板，模板一键带出分录（含科目不含金额）。

**过账/红冲**：过账动作执行 6.5 全部约束校验；红冲生成反向凭证（每行借贷互换、摘要前加"红冲："），两张凭证互链且原凭证 state→voided。

**期末结转（一键）**：选择期间 → 系统将损益类科目（6001/6051/6111/6301/6401/6402/6403/6601/6602/6603/6711/6801）余额结转到 3103 本年利润，生成结转凭证（source_type=closing）；重复执行同期间提示已结转。结账（period close）后期间锁定。

**账簿查询**：
- 总账：科目×期间的汇总（期初/本期借/本期贷/期末）；
- 明细账：科目下按凭证展开（日期/凭证号/摘要/借/贷/方向余额）；
- 科目余额表：全科目期初/发生/期末，支持期间区间，导出 Excel/CSV；
- 凭证连打：按期间连续打印凭证（打印样式：标准记账凭证版式，A4 两张/页）。

**财务报表**：资产负债表、利润表（小企业准则格式，公式取数映射存 `ir_config`，允许用户微调公式）；报表数字与账联查（点数字弹出构成明细）。

- **DoD**：① 每张凭证过账校验全通过（单测覆盖每个约束分支）；② 结转后损益科目余额为 0、本年利润正确；③ 资产负债表"资产=负债+权益"平衡校验内置（不平则报表顶部红条警示并显示差额）；④ 利润表净利润 = 本年利润科目发生额。

### 7.6 发票管理（invoice）

- 进项/销项两个列表页（Tab 切换）；表单录入（自动算价税合计）；发票影像上传（拍照/扫描件，进 doc_documents）。
- Excel 批量导入：模板下载 → 导入 → 预览校验（错误行标红说明）→ 确认入库；查重（同账套代码+号码+日期）。
- 月末视图：某期间进项合计/销项合计/税额合计（申报底稿取数来源）。
- **DoD**：导入 500 行 < 5 秒；重复发票拦截；价税合计自动计算精度正确（Decimal）。

### 7.7 自动记账引擎（auto_entry）—— 核心卖点

- 规则管理页：规则列表（启停/优先级/编辑）。
- **批量执行工作台**：选账套+期间 → 列出所有未生成凭证的发票/流水 → 逐条匹配规则（显示"将执行的规则+预览分录"）→ 勾选执行 → 批量生成 draft 凭证 + 执行日志。匹配不到规则的留在"待人工"清单。
- 幂等：已 entry_generated 的发票不再生成；失败整单回滚并记 auto_entry_log。
- **DoD**：种子规则全链路测试——给定 3 张销项票+2 张进项票+3 条流水，批量执行后生成 8 张平衡 draft 凭证且金额科目全部正确；同批重复执行不重复生成。

### 7.8 银行对账工作台（bank）

- 流水导入（Excel/CSV：银行导出格式适配常见工行/建行/招行模板，字段映射可配置存 ir_config）。
- 对账工作台：左右分栏——左：流水列表（未对账优先）；右：选中流水的凭证预生成区（命中规则自动带出，可改科目）；一键"生成凭证并标记已对账"。
- 按月对账汇总：期初余额 + 收入 - 支出 = 期末余额（与银行对账单核对）。
- **DoD**：导入含表头乱码（GBK/UTF-8 自动识别）容错；生成凭证后流水状态联动。

### 7.9 税务申报台账与日历（tax_decl）—— 防"漏报"的核心

- **申报日历页**（总览模式）：月历视图 + 列表视图切换。列表按月份展示：客户 × 税种 × 属期 × 截止日 × 状态（颜色：pending 灰 / preparing 蓝 / submitted 橙 / paid 绿 / exempt 白 / **逾期红**）。
- 台账行操作：①"计算"——打开申报底稿抽屉：显示系统计算的应纳税额与计算过程快照（例：增值税=销项合计-进项合计+上期留抵，取数自发票与台账，过程可追溯）；②"标记已申报"（填实缴额）；③"标记已缴款"（填缴款日期）；④"免税/零申报"（记录原因）。
- 官网直达：每税种提供电子税务局/自然人电子税务局等**外链按钮**（链接存 ir_config，用户可维护）——系统不做自动申报，只做导航与登记。
- 增值税计算底稿（M4 范围）：一般纳税人=当期销项-当期进项-上期留抵；小规模=含税收入/1.03×3%（季度 30 万免征判断可配置，默认按现行政策参数存 ir_config）。附加税=实缴增值税×税率。印花税=买卖合同计税依据×0.3‰（计税依据默认取当期购销合计，可手改）。企业所得税、个税底稿仅出取数汇总（营业收入/成本/利润总额），计算以用户在税局端为准。
- **DoD**：征期规则批量生成台账幂等且覆盖 100 账套 < 10 秒；逾期自动标红；每笔计算底稿可追溯取数来源（数字穿透到发票/凭证列表）。

### 7.10 外贸专区（foreign_trade，仅外贸账套可见）

- 报关单台账：列表+表单（含商品行子表）；单一窗口导出的报关明细 CSV 导入。
- 收汇台账：登记收汇/结汇，关联报关单（一笔报关多次收汇）。
- 出口退税台账：状态流转收集→申报→审批→到账。
- 平台链接中心：单一窗口 / 电子口岸 / 国家外汇管理局数字外管 / 电子税务局出口退税模块的直达链接 + 用户可编辑的操作备忘（文本）。
- **DoD**：非外贸账套看不到外贸菜单；报关单-收汇关联正确显示未收汇差额。

### 7.11 工资社保（payroll）

- 按月建批次：复制上月员工行（金额可改）→ 计算个税（累计预扣法，税率表可配置）→ 确认 → 一键生成两张凭证（计提：借 管理费用-工资 / 贷 应付职工薪酬；发放：借 应付职工薪酬 / 贷 银行存款，个税代扣进 应交税费-应交个人所得税）。
- 社保公积金台账：基数、单位/个人部分，随批次登记。
- **DoD**：个税计算单测（3 档税率边界值）；批次确认后凭证自动生成且平衡。

### 7.12 文档中心（documents）

- 全局文档列表（跨账套）：按客户/类型/标签/年份筛选；拖拽上传（多文件）；图片/PDF 在线预览。
- 业务单据挂接：发票、凭证、报关单表单内直接上传附件（自动登记到文档中心并双向关联）。
- **DoD**：上传 10MB PDF 正常；从发票页上传后文档中心可反查来源单据。

### 7.13 数据备份与计划任务（core/scheduler + base 设置页）

- **自动备份**：每日首次启动系统时 + 每日固定时刻（可配）执行：`PRAGMA integrity_check` → 备份 SQLite（VACUUM INTO）到 OneDrive 目录 `zhicai_backup/zhicai_YYYYMMDD_HHMM.db` → 保留最近 90 份自动清理 → 写备份日志（时间/大小/结果）。
- 手动"立即备份"按钮（系统设置页）+ 备份历史列表 + "从备份恢复"引导（说明文档式，不自动执行覆盖）。
- 计划任务框架（对标 Odoo ir.cron）：任务注册（backup / decl_generate 台账生成 / fee_overdue 逾期扫描 / task_reminder 到期提醒），每日 00:05 服务器启动后补跑错过的任务；执行留痕。
- **DoD**：备份文件可用（恢复到临时库校验通过）；90 份滚动清理正确；日志可查。

### 7.14 系统设置（base）

- 用户管理（预留多用户）、修改密码；
- 参数配置（ir_config 可视化编辑：个税税率表、免征额参数、科目映射表、银行导入模板映射、官网链接等）；
- 审计日志查看（按用户/模型/时间筛选）；
- 数据维护：数据库健康检查、备份入口、科目模板管理、导出全量数据（CSV 包）。

---

## 8. WebUI 设计规范（借鉴 Odoo 18 的界面范式）

### 8.1 整体布局

```
┌──────────┬──────────────────────────────────────────────┐
│          │  顶栏：面包屑 …… [全局搜索] [账套:深圳XX贸易] [👤] │
│  左侧    ├──────────────────────────────────────────────┤
│  导航栏   │                                              │
│  (折叠↔  │              主内容区                          │
│  展开,    │   （列表 / 表单 / 看板 / 日历 / 报表 / 工作台）  │
│  220px↔  │                                              │
│  64px)   │                                              │
└──────────┴──────────────────────────────────────────────┘
```

### 8.2 通用视图组件行为（全部由视图配置驱动）

**ListView（列表）**：
- 工具栏：新建 / 导入 / 导出 CSV / 筛选器（chips 式，可组合）/ 分组下拉（按任意字段分组折叠）/ 搜索框；
- 表格：列头点击排序（升/降图标）；首列多选框；底部"80 条/页"分页 + 总数；
- 行操作：悬浮"打开/编辑"按钮；选中多条时顶部出现批量操作条；
- 空态：插画 + "暂无数据，点击新建"。

**FormView（表单）**：
- 页头：图标 + 记录显示名（rec_name）+ **状态徽章**（彩色圆点：draft 灰/posted 绿/逾期 红 等）+ 右侧主操作按钮组（保存/过账/红冲/打印——按 state 动态显示）；
- 内容：标签页分组字段；两列栅格布局；One2many 子表（表格内嵌编辑）；
- 页底：**活动日志面板**（chatter：时间线显示字段变更[旧值→新值]与备注留言，可添加备注）。

**KanbanView（看板）**：按 state 列分栏，卡片显示标题+关键字段+头像（客户 initials），支持拖拽换列（改 state）。用于：任务、退税台账、收费状态。

**CalendarView（日历）**：月视图为主，事件=申报截止日/任务到期日，颜色按税种/优先级。

**DashboardView**：卡片栅格（2/4 列自适应），每卡片标题+数字/图表/列表三种形态。

### 8.3 交互细节（对齐 Odoo 手感）

- 所有写操作即时 toast 反馈（成功绿/失败红，失败信息可展开技术详情）；
- 危险操作（红冲、结账、停用客户）二次确认弹窗，确认文案说明后果；
- 凭证/发票等数字字段千分位显示，录入时仅数字；
- 全键盘：列表 `n`=新建、`/`=聚焦搜索、`Esc`=关闭弹窗；凭证录入页 Tab/Enter 流转（类 Excel）;
- 加载态：路由切换显示顶部进度条；列表数据骨架屏；
- 响应式：≥1200px 标准布局；<900px 左侧导航自动收起为图标（手机浏览器可看仪表盘/日历/任务）。

### 8.4 视觉规范

| 项 | 值 |
|----|----|
| 主色 | Odoo 紫 `#714B67`（导航栏底色、主按钮、选中态） |
| 辅助色 | 成功 `#28A745` / 警告 `#F59E0B` / 危险 `#DC3545` / 信息 `#17A2B8` |
| 背景 | 页面 `#F9F9F9`，卡片白底 `#FFFFFF` + 1px 边框 `#E5E5E5` + 微圆角 4px |
| 文字 | 主 `#212529`，次要 `#6C757D` |
| 字体 | 系统栈：`"Segoe UI", "Microsoft YaHei", sans-serif`；正文 14px，表格 13px |
| 状态徽章 | 圆点 8px + 文字，颜色映射统一放 `js/common/states.js` |

> 浅色主题为唯一主题（单用户本机使用，不做暗色模式）。

---

## 9. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 列表接口 P95 < 300ms（10 万行数据量下）；仪表盘 < 1s；启动（含依赖检查）< 10s |
| 容量 | 100 账套 × 10 年数据（见 2.3）无 degradation；附件目录 10GB+ |
| 可靠性 | 所有业务动作事务化（失败回滚）；SQLite WAL；每日自动备份（7.13）；断电后重启自检 |
| 安全 | 密码 pbkdf2 存储；会话签名 cookie（HttpOnly）；仅监听 127.0.0.1（默认不出本机）；操作全审计 |
| 可维护性 | 模块高内聚低耦合（模块间只通过 service 层 import 依赖模块的 service，禁止跨模块直查表）；Alembic 迁移每次表结构变更必提交 |
| 可测试性 | 核心逻辑（domain 解析/凭证校验/结转/算税/规则引擎）单测覆盖率 ≥ 80%；每模块含 API 集成测试 |
| 兼容 | Chrome/Edge 最新版（本机场景）；Windows 10/11 |

---

## 10. 开发路线图（里程碑与验收）

> 每个里程碑 = 一次 Codex 会话可完成的任务包。**必须按序交付**。每个里程碑的 DoD 未全部达成不得开始下一个。

### M1 — 基座与客户管理（把"Odoo 式框架"立起来）
**范围**：项目骨架（5.2 目录结构全量建立）、FastAPI 启动、SQLite + Alembic、登录/会话、模块注册表、domain 解析器、动态 CRUD API + meta API、前端 AppShell（导航/顶栏/账套切换器/全局搜索）、ListView + FormView 通用渲染器、base 模块（用户/角色/配置/审计/note）、res_book 客户管理（新建向导含科目初始化）+ res_partner、种子数据（admin 账号、示例账套 2 个）。
**验收**：① `start.bat` 一键起服务并自动开浏览器；② 新建客户向导产出完整科目表+账簿；③ 客户列表筛选/搜索/排序可用；④ 任意表单保存后活动日志记录字段变更；⑤ domain 单测全绿；⑥ pytest 覆盖 CRUD API。

### M2 — 记账核心（account 模块全量）
**范围**：account 全模型、凭证录入定制页（Excel 式录入+模板）、过账/红冲、期末结转、总账/明细账/科目余额表、资产负债表/利润表、凭证打印、期间锁账。
**验收**：7.5 的 DoD 全部达成 + 凭证录入页支持 `//` 摘要复制与科目联想。

### M3 — 发票与自动记账（invoice + auto_entry + bank 导入）
**范围**：invoice_bill 全量（含 Excel 导入/查重/影像挂接）、auto_entry 规则引擎 + 批量执行工作台 + 种子规则、bank 流水导入 + 对账工作台。
**验收**：7.6/7.7/7.8 的 DoD 全部达成。

### M4 — 税务申报（tax_decl）
**范围**：征期规则 + 台账批量生成、申报日历（日历/列表双视图）、计算底稿（增值税/附加/印花税/所得税取数汇总）、状态流转、官网链接中心。
**验收**：7.9 的 DoD 全部达成；100 账套台账生成压力测试通过。

### M5 — 合同收费与任务（contract + tasks + board）
**范围**：合同/收费计划/收款登记/应收报表、任务管理（看板）、首页仪表盘全部卡片、逾期/到期扫描计划任务。
**验收**：7.2/7.4 DoD 达成；仪表盘数字与明细穿透一致。

### M6 — 外贸与工资（foreign_trade + payroll + documents）
**范围**：外贸三台账 + 平台链接中心、工资批次/个税/凭证生成、文档中心（上传/预览/挂接）。
**验收**：7.10/7.11/7.12 DoD 达成。

### M7 — 加固与收尾（backup + 设置 + 性能）
**范围**：自动备份/备份历史/恢复引导、计划任务框架完善、系统设置页全量、性能优化（索引核查/慢查询）、README 终稿（含运维手册：备份恢复/常见问题/数据目录说明）。
**验收**：7.13/7.14 DoD 达成；2.3 数据量模拟压测达标（脚本造 10 年数据跑报表）。

---

## 11. Codex 开发指引

### 11.1 工程规范

- Python：类型标注全覆盖；service 层抛业务异常（自定义 `BizError(code, message)`），API 层统一转 HTTP 400/409；禁止在路由层写业务逻辑。
- 前端：ES Modules，无构建；组件文件 = 单文件 Vue 组件（`Vue.defineComponent` + 模板字符串）；通用渲染器与业务定制页分离（`components/` vs `modules/`）。
- Git：里程碑一次或多次提交均可，提交信息格式 `M2: 凭证录入页与过账校验`。
- 时间处理：业务日期一律 naive date（字符串 `YYYY-MM-DD` 出入参）；金额 Decimal，JSON 序列化为字符串。
- 错误信息面向用户可读（中文），日志面向开发者（含堆栈）。

### 11.2 分阶段投喂方法（怎么用这份项目书）

每个里程碑开新会话时，给 Codex 的提示词模板：

```
你是一名资深全栈工程师。请严格依据《智财代账工作台项目书》实施 M< n > 里程碑。
项目书内容如下：【粘贴本文档全文（或让 Codex 读取仓库 docs/ 下的项目书文件）】
当前仓库状态：<附 git log --oneline -5 或说明"从零开始">
要求：
1. 只做 M< n > 范围，不要实现后续里程碑功能；
2. 完成后运行全部测试并修复至全绿；
3. 在 README"假设与决策记录"登记你做的假设；
4. 输出变更清单与 M< n > 验收标准逐条自检结果。
```

### 11.3 全局验收清单（最终交付时逐条打勾）

> **2026-09-02 终验：M1–M7 全部交付，以下逐条打勾（含验证证据）。**

- [x] start.bat 一键启动，断网可用（本地 vendor 回退）— M1 验收通过；前端已去除全部 CDN 依赖，Vue/ElementPlus/图标/语言包均走 `static/vendor/` 本地副本
- [x] 100 账套 × 10 年模拟数据下报表与列表性能达标（9 章）— `tools/perf_seed.py` 压测：458,640 凭证 / 1,834,560 分录 / 309MB，7 接口 P95 全部远低于预算（列表 ~13ms、余额表 ~20ms、仪表盘 ~34ms）
- [x] 借贷平衡/期间锁/红冲互链等会计约束单测全绿 — `test_account_m2.py::TestPostValidation` 覆盖 6.5 全部约束分支；全量 182 测试通过
- [x] 自动记账端到端演示：导入发票+流水 → 批量生成平衡凭证 → 过账 → 报表数字正确 — `demo_data_m3.py`（3 销项 + 2 进项 + 3 流水 → 8 张平衡凭证）；`test_m3.py::test_full_chain_8_moves` 断言科目与金额
- [x] 申报台账全税种演示：生成→计算→标记→逾期标红 — `demo_data_m4.py` + `test_m4.py`（幂等 / 100 账套 0.1s / 逾期 6 种组合）
- [x] 备份文件恢复演练通过 — `test_m7.py::TestBackup::test_backup_ok_file_is_valid_sqlite`（备份文件可用 sqlite3 打开并含全部注册表）；线上手动触发备份已落 OneDrive（integrity=ok）
- [x] README 完整（安装/启动/备份/恢复/FAQ）— README 含快速开始、M7 运维手册（开启备份三步骤 / 灾难恢复六步骤 / 常见问题 / 数据目录说明 / 压测结论）

---

## 12. 附录

### 附录 A：Odoo → 本项目借鉴对照速查

| Odoo 机制 | Odoo 出处 | 本项目落点 |
|-----------|----------|-----------|
| 模块 = manifest + models/views/security | `__manifest__.py` | `app/modules/<m>/` + ModuleDescriptor |
| 模型驱动视图（XML） | `views/*.xml` | `views.py` 视图配置 dict |
| recordset domain 语法 | `models.Model.search()` | `core/domain.py` |
| 多公司 company_id + record rules | `res.company` | `res_book` + book_context 中间件 |
| mail.thread chatter | `mail.thread` mixin | audit_log + note_message |
| ir.sequence | `ir_sequence` 表 | `core/sequence.py` |
| ir.cron | `ir_cron` 表 | `core/scheduler.py` |
| 发票=account.move(move_type) | `account.move` | invoice_bill(direction) + account_move(source_type) |
| 银行对账规则 reconcile model | `account.reconcile.model` | auto_entry_rule |
| soft delete active 字段 | 全模型 `active` | BaseModel.active |

### 附录 B：关键业务参数默认值（存 ir_config，界面可改）

| 参数 | 默认值 |
|------|--------|
| `payroll.iit.brackets` | 累计预扣率表（3%/10%/20%/25%/30%/35%/45% 七级，现行标准） |
| `tax.vat.small.quarterly_free_limit` | 300000（季度免征额，可改 0 关闭） |
| `tax.stamp.rate.purchase_sale` | 0.0003 |
| `tax.surtax.rates` | 城建 0.07 / 教育附加 0.03 / 地方附加 0.02 |
| `backup.dir` | OneDrive 目录（首次向导设置） |
| `backup.keep_copies` | 90 |
| `entry.category_map` | 发票 category → 科目编码映射（预置 20 类常用） |
| `portal.links` | 电子税务局/单一窗口/电子口岸/数字外管 URL |

### 附录 C：常用发票类别 → 科目映射（种子）

办公用品→6602.01 管理费用-办公费；差旅→6602.02；业务招待→6602.03；房租→6602.04/或长期待摊；水电物业→6602.05；通讯网络→6602.06；广告宣传→6601.03 销售费用；运输物流→6601.04；材料采购→1403 原材料/1405 库存商品（按客户行业选择）；固定资产类→1601 固定资产；咨询服务→6602 管理费用。

### 附录 D：术语表

| 术语 | 含义 |
|------|------|
| 账套（book） | 一家客户公司的独立核算单元，对标 Odoo 的 company |
| 凭证（move） | 记账凭证，借贷分录集合 |
| 过账（post） | 凭证生效进入账簿，之后不可修改 |
| 红冲 | 生成反向凭证冲销原凭证 |
| 结转损益 | 期末将损益类科目余额结至本年利润 |
| 申报底稿 | 系统计算的应纳税额过程快照 |
| 属期 | 税款所属期间（如 2026-08） |
| 征期 | 申报期限（如次月 15 日前） |

---

**文档结束。** 本项目书为 v1.0 基线；实施过程中发现的补充需求一律以"变更记录"形式追加在本章之后，不得直接改动已验收章节内容。

---

## 13. 变更记录（实施期间追加）

### CR-001（2026-09-02）：开发模式变更
原定"每个里程碑 = 一次 Codex 会话"改为 **AI 直接开发**（用户决定，见第 11 章指引废弃说明）。里程碑顺序 M1→M7 与各章 DoD 验收标准不变，每阶段仍逐条过 DoD 后才进入下一阶段。

### CR-002（2026-09-02，M1 实施决策）
- 数据库建表用 `create_all`，**Alembic 未引入**（从零建库无存量迁移需求；待真有表结构变更时再引入）。
- 企业会计准则科目模板暂映射小企业模板（6.6 只给了小企业明细；当前无企业准则客户）。
- 前端去除全部 CDN 依赖（含 unpkg），Vue/ElementPlus/图标/语言包全走 `static/vendor/` 本地副本，保证断网与国内网络环境秒开。
- admin 初始密码 `admin123` 写死种子（单机单用户），README 与登录页提示首次修改。

### CR-003（2026-09-02，M2 实施决策）
- `account_move_line.account_id` 允许为空：草稿录入允许暂存空科目，过账时再强制校验（原设计录入即校验，改动以支持"先记金额后补科目"的实操习惯）。
- 凭证打印暂未做独立打印页，账簿查询/报表页自带浏览器打印（Ctrl+P）可用；如需专业凭证打印格式后续追加。

### CR-004（2026-09-02，M4 实施决策）
- 传月度属期（如 2026-09）自动展开同属期季度项（2026Q3 季度税种），否则按季税种会被整体漏掉——原 7.9 未明确此边界。
- 企业所得税与个税**只出取数汇总不做计算**（计算以税局端为准），与 6.4 口径一致。

### CR-005（2026-09-02，M5 实施决策）
- 系统自动催收/续约任务优先级为 `high`（重要），`urgent`（紧急）保留给用户手工创建的任务，避免系统任务淹没仪表盘待办列表。

### CR-006（2026-09-02，M6 实施决策）
- 工资批次种子自动记账规则默认**停用**（需手工启用），避免无工资客户账套误生成凭证。
- 文档中心单文件上限 10MB、扩展名白名单（图片/PDF/Office/zip），拒绝可执行文件。

### CR-007（2026-09-02，M7 实施决策）
- 恢复操作为**引导文档式**（设置页六步骤说明 + README），不做自动恢复按钮——误操作覆盖生产库的风险远大于便利收益。
- 自动备份**只备主库** `zhicai.db`；附件目录 `data/attachments/` 不含在内（README 已注明手动复制，或将 attachments_dir 配到 OneDrive）。
- 实施时将 `backup_dir` 实配为 `C:/Users/weiqi/OneDrive/财务系统备份`，线上已验证每日 09:00 自动备份 + 手动触发均正常。
- 附录 A 中 `core/scheduler.py` 的实际落点为 `app/modules/maintenance/cron_service.py`（ir.cron 对标实现，含启动补跑与执行留痕）。

### 交付终态（2026-09-02）
M1–M7 全部完成：**182 个 pytest + 43 项 jsdom 冒烟全绿**；100 账套 × 10 年压测全达标；备份链路（OneDrive）线上验证通过。11.3 全局验收清单已逐条打勾。
